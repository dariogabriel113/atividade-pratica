import styled from "styled-components";

export const MainMenuContainer = styled.nav`
    ul {
        list-style: none;
        padding-top: 16px;

        li {
            display: inline-block;
            margin-right: 3%;
        }
    }
`